name: checking versions of node and npm
on: push
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: checking the version of node
        run: node --version
  second-job:
    runs-on: windows-latest
    steps:
      - name: checking the version of npm
        run: npm --version
